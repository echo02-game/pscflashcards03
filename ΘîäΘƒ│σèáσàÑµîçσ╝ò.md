# 如何加入輕聲及兒化韻錄音

本學習工具已預留錄音播放功能。難點詞語仍使用系統 AI 語音示範；輕聲及兒化韻詞語可改用你錄製的音頻。

## 建議錄音格式

- 建議使用 `.mp3`，手機和電腦兼容性最好。
- 每個詞語建議一個音檔，例如：
  - `guiju.mp3`
  - `shihou.mp3`
  - `fengzheng.mp3`
- 檔名建議只使用英文、數字、連字符 `-` 或底線 `_`，避免空格和中文檔名。

## 加入步驟

1. 在網站資料夾中打開 `audio` 資料夾。
2. 把錄好的音檔放入 `audio` 資料夾，例如：

   ```text
   audio/guiju.mp3
   audio/shihou.mp3
   audio/fengzheng.mp3
   ```

3. 打開 `audio-map.js`。
4. 在 `window.AUDIO_MAP = { ... }` 裡加入詞語和音檔路徑，例如：

   ```js
   window.AUDIO_MAP = {
     "規矩": "./audio/guiju.mp3",
     "時候": "./audio/shihou.mp3",
     "風箏": "./audio/fengzheng.mp3"
   };
   ```

5. 儲存 `audio-map.js`。
6. 把新增的音檔和更新後的 `audio-map.js` 上載到 GitHub。
7. 如果想讓使用者更快取得新版，打開 `sw.js`，把：

   ```js
   const CACHE_NAME = "psc-word-card-v8";
   ```

   改成下一個版本，例如：

   ```js
   const CACHE_NAME = "psc-word-card-v9";
   ```

8. 重新上載 `sw.js`。

## 注意事項

- 錄音只需要加入輕聲及兒化韻詞語。
- 如果某個詞語未在 `audio-map.js` 加入音檔，網頁會顯示「錄音稍後提供」。
- 若希望錄音離線可用，使用者需要在有網絡時先播放一次該錄音；瀏覽器會嘗試快取已播放的音檔。
- 若要「首次載入後所有錄音都離線可用」，需要把所有音檔路徑加入 `sw.js` 的 `ASSETS` 清單，但音檔較多時首次載入會變慢。
